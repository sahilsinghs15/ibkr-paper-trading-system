# Critical code audit — 2026-08-27

Scope: `/home/tradingapp/app` plus repo-root operational files.
Method: code read first. Docs (`AGENTS.md`, `app/docs/*`, architecture markdown) deliberately
**not** consulted for this phase. Every claim below cites `path:line`. Line numbers refer to the
**on-disk working tree**, which is dirty relative to `HEAD` (see F-01).

Paths are relative to `/home/tradingapp/app/backend` unless prefixed otherwise.

---

## Phase 1 — The real map

### 1.1 Entry points

Eight distinct process entry points exist. Only two are long-lived servers.

| # | Entry point | Invocation found at | Kind |
|---|---|---|---|
| E1 | `app.main:app` (uvicorn) | `/home/tradingapp/app/scripts/process_manager.py:308`, `/home/tradingapp/start.txt:1` | HTTP server + background tasks |
| E2 | `demo_streaming` (`python -m demo_streaming`) | `demo_streaming/__main__.py:1-4`, `/home/tradingapp/start.txt:9` | HTTP server + poller |
| E3 | `process_manager.py` | `/home/tradingapp/app/scripts/process_manager.py:671-672` | Supervisor (spawns Xvfb, IB Gateway, E1) |
| E4 | `alembic` | `alembic/env.py:1-95`, `alembic.ini:8` | Migration CLI |
| E5 | `pytest` | `pyproject.toml:40-42` | Test runner |
| E6 | `scripts/*.py` (7 scripts) | e.g. `scripts/oms/run_paper_execution.py`, `scripts/oms/flatten_gateway_positions.py`, `scripts/load_test_mft_burst.py`, `scripts/prune_webhook_captures.py`, `scripts/repair_historical_killswitch_positions.py`, `scripts/test_tws_connection.py`, `scripts/test_tws_market_data.py` | Ad-hoc CLI |
| E7 | `scripts/instrument_master/*` | `scripts/instrument_master/discover.py` (1206 lines), `seed_fetcher.py`, `seed_paper_cfd.py`, `discover_cfd.py` | Offline data tool |
| E8 | `scratch/*.py` (5 files) | `scratch/inspect_db_state.py`, `scratch/count_allocations.py`, `scratch/inspect_target_signal.py`, `scratch/check_ib_gateway_connection.py`, `scratch/verify_live_market_data.py` | Throwaway diagnostics, committed to git |

There is **no container entry point for the application**. `app/docker-compose.yml:1-19` defines
only a `postgres:17` service; nothing containerises E1 or E2.

There is **no scheduler/cron entry point.** Everything periodic runs as an in-process asyncio task
inside E1 (see 1.2).

#### E1 — `app.main:app`

Constructed by `create_app()` at `app/main.py:168-195`; module-level instantiation at
`app/main.py:198`. Startup work is in the `lifespan` context manager, `app/main.py:31-165`.

What it starts, in order:

| Order | Object | Line | Notes |
|---|---|---|---|
| 1 | `setup_logging` | `app/main.py:38` | writes `/home/tradingapp/storage/logs/{date}/trading.log` (`app/core/logger.py:19,33-36`) |
| 2 | `TWSClient` | `app/main.py:42` | not yet connected |
| 3 | `GatewayRateLimiter` | `app/main.py:43-50` | registered onto client at `:50` |
| 4 | `IBKRExecutionAdapter` | `app/main.py:51-58` | registers itself as TWS listener at `app/oms/ibkr_adapter.py:95` |
| 5 | `OMSService` | `app/main.py:59` | |
| 6 | `ModelBlueExecutionPersistence`, `OrderManager` | `app/main.py:61-71` | `OrderManager.__init__` builds `BasketCoordinator` at `app/services/order_manager.py:160-169` |
| 7 | `LivePnlService` | `app/main.py:72-74` | assigned to the private `order_manager._live_pnl`; registers as market-data listener at `app/services/pnl.py:110-111` |
| 8 | `hydrate_runtime_from_db()` | `app/main.py:76` | `app/services/order_manager.py:171-215` |
| 9 | `CriticalRecoveryService` | `app/main.py:80-88` | |
| 10 | `client.connect_and_start(...)` | `app/main.py:91-96` | opens the TCP socket to TWS and starts `TWSClientThread` (`app/broker/ibkr/tws_client.py:531-532`) |
| 11 | `hydrate_live_pnl()` | `app/main.py:101` | only on successful connect |
| 12 | `RecoveryManager.run_startup_recovery()` | `app/main.py:116-118` | |
| 13 | `ExecutionWorkerPool` (10 workers + 1 reclaimer) | `app/main.py:123-129` | `app/services/worker_pool.py:85-97` |
| 14 | `PositionReconciler` (1 task) | `app/main.py:131-133` | `app/services/position_reconciler.py:266-270` |
| 15 | `critical_recovery.enqueue_all_critical()` | `app/main.py:135` | spawns per-basket tasks (`app/services/critical_recovery.py:107`) |

What it listens to:

- HTTP: `/health` (`app/api/routes/health.py:8`), `/api/webhooks/*` (`app/main.py:192`),
  `/api/v1/*` (`app/main.py:193` → `app/api/router.py:14-19`).
- IBKR TWS socket callbacks on the `TWSClientThread`: `app/broker/ibkr/tws_client.py:73-371`
  (`nextValidId`, `error`, `orderStatus`, `openOrder`, `execDetails`, `commissionReport`,
  `position`, `tickPrice`, `contractDetails`, …), fanned out to two listener lists
  (`app/broker/ibkr/tws_client.py:37-38`).
- PostgreSQL queue polling: the 10 worker tasks poll `signal_jobs` every 0.5 s
  (`app/services/worker_pool.py:150-155`, default `idle_poll_interval_sec=0.5` at `:62`).

Bind address: `127.0.0.1:8000` (`/home/tradingapp/app/scripts/process_manager.py:57-58`);
exposed publicly through ngrok per `/home/tradingapp/start.txt:3`.

#### E2 — `demo_streaming`

`demo_streaming/__main__.py:1-4` → `demo_streaming/main.py:99-100` → `_serve()` at
`demo_streaming/main.py:30-96`.

Starts: its own SQLAlchemy engine (`demo_streaming/main.py:33-34`, independent of
`app/db/session.py`), a Redis client (`:36-41`), a `PositionBridge` poller task (`:75`), a shutdown
watchdog task (`:76`), and a uvicorn server (`:67-74`). It hard-fails if Redis is unreachable
(`:50-52`).

Listens on `127.0.0.1:8010` by default (`demo_streaming/config.py:16-17`), but
`/home/tradingapp/start.txt:9` runs it with `DEMO_STREAM_HOST=0.0.0.0`.

Routes: `/health`, `/demo/positions`, `/demo/closed-positions`, `/demo/signals`,
`/demo/market-data-health`, `/demo/stream` (SSE), SPA routes, and — critically — a catch-all
reverse proxy `ANY /api/v1/{full_path:path}` at `demo_streaming/api.py:210-241`. See F-02.

Does **not** connect to IBKR (consistent with its docstring at `demo_streaming/main.py:1`).

#### E3 — `process_manager.py`

`/home/tradingapp/app/scripts/process_manager.py`. Supervises three children:
`Xvfb` (`:226-227`), IB Gateway via IBC (`:294-303`), and E1 (`:306-311`). Gating logic: FastAPI is
started only once IBC logs `Login has completed` **and** TCP `127.0.0.1:4002` accepts (`:492-501`,
`:335-396`). Restart budget: 5 per 600 s (`:89-90`).

#### E4 — `alembic`

`alembic/env.py` imports `app.core.config` and `app.db.base`/`app.db.models` (confirmed via import
graph). Chain is linear with a single head: `d4bd73bb4fde` → … → `a1b2c3d4e567` (19 revisions).
No branches, no multiple heads.

---

### 1.2 The actual signal path, hop by hop

#### Ingress (synchronous, HTTP request thread)

| Hop | Location |
|---|---|
| 1 | `POST /api/webhooks/tradingview` → `app/api/routes/webhooks.py:166-173` |
| 2 | Shared-secret check → `app/api/routes/webhooks.py:176` → `_verify_webhook_authentication` at `:144-161` |
| 3 | Body read + JSON parse → `:178-201` |
| 4 | `request_id = uuid4()`, log context bound → `:203-204` |
| 5 | `_process_tradingview_webhook` → `:213` |
| 6 | `compute_idempotency_key(payload)` → `app/services/worker_pool.py:27-47` (SHA-256 over `strategy_id:signal_id:action`, `:45-46`) |
| 7 | session factory looked up off `app.state` → `app/api/routes/webhooks.py:235-246` |
| 8 | `SignalJobRepository.create_job_if_not_exists` → `app/db/repositories/signal_repository.py:302` — **this is the durability boundary** |
| 9 | `HTTP 202` returned with `signal_id`/`job_id` → `app/api/routes/webhooks.py:298-304` |
| 10 | Fire-and-forget CSV append on a thread → `:282-296` → `_append_incoming_signal_csv` at `:127-141` |

No execution happens in the request. The endpoint is a durable-enqueue only.

#### Dequeue and execution (asyncio worker tasks)

| Hop | Location |
|---|---|
| 11 | `_worker_loop` polls → `app/services/worker_pool.py:148-163` |
| 12 | `_claim_job` → `:165-171` → `SignalJobRepository.claim_next_jobs` → `app/db/repositories/signal_repository.py:347` |
| 13 | `_process_claimed_job` acquires per-`(account_scope, strategy_id)` asyncio lock → `app/services/worker_pool.py:173-189` (lock factory `:77-83`) |
| 14 | Lease heartbeat task spawned → `:184-185`, loop at `:210-245` |
| 15 | `_execute_job` writes `PROCESSING` with a fence → `:277-295` |
| 16 | `order_manager.parse_inbound_payload` → `app/services/order_manager.py:339-353` → `parse_tradingview_payload` in `app/services/strategies/inbound.py` |
| 17 | `order_manager.process_signal_execution` → `app/services/order_manager.py:366` |
| 18 | inbound signal row + 2 audit events persisted → `:378` → `_persist_inbound_signal` at `:1004-1072` |
| 19 | `_process_signal_execution_inner` → `:402`; strategy handler lookup `:406`; account resolution `:423` |
| 20 | `_fanout_accounts` → `:494-509` (concurrent `asyncio.gather` over accounts, `:508`) |
| 21 | `_fanout_single_account` → `:437`; `handler.build_intent` `:451`; kill-switch gate `:452-462` |
| 22 | `_evaluate_and_submit` acquires per-`(account, symbol)` exposure locks → `:678-690`, guard at `:651-676` |
| 23 | `_evaluate_and_submit_locked` → `:692`; zero-qty guard `:702-707` |
| 24 | `RMSEngine.evaluate` → `:709` → `app/rms/engine.py`; 5 checks in `app/rms/checks/` |
| 25 | RMS audit event → `:710` → `_audit_rms` at `:803-840` |
| 26 | `_resolve_instruments` → `:717` → `:1094-1138`; CFD discovery `:1116-1122`; `attach_resolved` `:1128` |
| 27 | `BASKET_CRITICAL` gate → `:724-738` (`app/oms/coordinator.py:88-91`) |
| 28 | Durable dedupe claim, own transaction → `:744` → `_acquire_execution_claim` at `:581-601` |
| 29 | `BasketCoordinator.execute` → `:748` → `app/oms/coordinator.py:195` |
| 30 | `attach_resolved` again → `app/oms/coordinator.py:205`; basket row persisted `:219` |
| 31 | per-leg `OMSService.submit_one_leg` → `app/oms/coordinator.py:266` → `app/oms/oms_service.py:162-199` |
| 32 | `OMSService._submit_leg` → `app/oms/oms_service.py:201-293`; order-id minting `:215`; `resolve_leg` fallback `:224-230` |
| 33 | `IBKRExecutionAdapter.submit_order` → `app/oms/oms_service.py:283` → `app/oms/ibkr_adapter.py:206` |
| 34 | rate-limiter token → `app/oms/ibkr_adapter.py:213` → `_acquire_for_order` at `:190-204` |
| 35 | contract + order built → `:225-226`; id maps registered *before* send → `:230-235` |
| 36 | **`client.placeOrder(...)` → TWS socket** → `app/oms/ibkr_adapter.py:257` |
| 37 | `_persist_child` (order row + executions) → `app/oms/coordinator.py:329` → `:968-1001` |
| 38 | `_wait_terminals` → `app/oms/coordinator.py:327` → `:551-575` → `adapter.wait_for_terminal_or_fill` at `app/oms/ibkr_adapter.py:336` |
| 39 | completeness check → `app/oms/coordinator.py:351` → retry (`:352`) / compensate (`:820`) / `_fail_critical` (`:912`) |
| 40 | basket settles `OPEN`/`CLOSED` → `app/oms/coordinator.py:364-374` |
| 41 | claim sealed → `app/services/order_manager.py:768` → `_seal_execution_claim` at `:603-611` |
| 42 | in-memory RMS state updated → `:770` → `_update_runtime_state` at `:925-976` |
| 43 | live P&L subscribe/unsubscribe → `:773-778` |
| 44 | terminal job status written under fence → `app/services/worker_pool.py:359-361` → `_write_status` at `:247-275` |

#### Broker callback path (TWS reader thread → asyncio loop)

| Hop | Location |
|---|---|
| C1 | `TWSClient.orderStatus` / `execDetails` / `commissionReport` → `app/broker/ibkr/tws_client.py:281`, `:340`, `:362` |
| C2 | fan-out to `self._listeners` → `:309`, `:343`, `:365` |
| C3 | `IBKRExecutionAdapter` (registered at `app/oms/ibkr_adapter.py:95`) updates order state, resolves `_fill_futures` (`:82-84`) |
| C4 | `_emit_order_state` → `app/oms/ibkr_adapter.py:101-106` |
| C5 | `BasketCoordinator._on_broker_order_state` (registered at `app/oms/coordinator.py:83-86`) → `:1084`; hops threads via `self._loop` captured at `:203` |
| C6 | `_persist_broker_snapshot` → `app/oms/coordinator.py:1119` |
| C7 | market-data ticks → `app/broker/ibkr/tws_client.py:152-188` → `LivePnlService.on_tick_price` at `app/services/pnl.py:350` → `_recompute` `:728` → `asyncio.run_coroutine_threadsafe` `:751` |

#### Periodic loops inside E1

| Loop | Interval | Location |
|---|---|---|
| Stale job / claim reclaimer | 15 s | `app/services/worker_pool.py:117-146` (`:60`) |
| Lease heartbeat (per in-flight job) | `max(2.0, lease/3)` = 10 s | `app/services/worker_pool.py:210-245` |
| Position reconciler | `self._interval_sec` | `app/services/position_reconciler.py:290-301` |
| Critical-basket recovery retries | `RECOVERY_RETRY_DELAY_SEC` | `app/services/critical_recovery.py:141-176` |

---

### 1.3 Module inventory and reachability

114 modules under `app/`. Import graph computed with AST, including implicit parent-package
imports and relative-import resolution.

**Reachable from `app.main`: 113 of 114.**

**Not reachable from any server entry point (1):**

- `app/instruments/paper_cfd_catalog.py` — imported only by
  `scripts/instrument_master/seed_paper_cfd.py`. Contains two hard-coded IBKR conIds
  (`:15-16`, `:26-27`).

Other entry-point trees touch a much narrower slice of `app/`:

- **E2 (`demo_streaming`)** reaches only 21 `app.*` modules: `app.core.config`, `app.core.logger`,
  `app.db.base`, `app.db.session`, and all 14 `app.db.models.*`. It does not import any
  service, OMS, RMS, or broker module. Its own data access lives in
  `demo_streaming/snapshot.py` (871 lines).
- **E4 (`alembic`)** reaches the same `app.db.*` slice plus `app.core.config`.

**Modules with exactly one importer in `app/`** (single-consumer; candidates for inlining or signs
of an abandoned abstraction):

| Module | Sole `app/` importer |
|---|---|
| `app/core/identifiers.py` | `app/services/worker_pool.py:12` |
| `app/services/recovery.py` | `app/main.py:112` |
| `app/services/strategies/legacy.py` | `app/services/strategies/inbound.py` |
| `app/schemas/webhook.py` | `app/api/routes/webhooks.py:19` |
| `app/schemas/critical_basket_schemas.py` | `app/api/routes/baskets.py:14` |
| `app/services/model_blue/db_allocation.py` | `app/main.py:21` |
| `app/db/models/execution_claim.py` | `app/db/repositories/execution_claim_repository.py` |
| `app/db/models/execution_settings.py` | `app/accounts/config_service.py` |
| `app/db/models/broker_position.py` | `app/db/repositories/broker_position_repository.py` |

**Package `__init__.py` re-export facades that no `app/` module consumes** — every internal
consumer imports the concrete module instead. These exist purely for tests and scripts:

- `app/db/__init__.py:3-39` (12 re-exports) — internal code imports `app.db.models.x` directly.
- `app/broker/__init__.py:3-6` and `app/broker/ibkr/__init__.py:3-5` — duplicate re-exports of
  the same `TWSClient`; internal code imports `app.broker.ibkr.tws_client`.
- `app/rms/__init__.py:3-30` (11 re-exports) — consumed only by
  `scripts/oms/run_paper_execution.py` and 6 test modules.
- `app/services/__init__.py:3-6` — consumed by nothing in `app/`.
- `app/accounts/__init__.py`, `app/schemas/__init__.py`, `app/services/model_blue/__init__.py`,
  `app/services/strategies/__init__.py`, `app/rms/checks/__init__.py`, `app/db/models/__init__.py`
  — same pattern.

**Non-`app/` trees not exercised by any server:**

- `scripts/instrument_master/` — 1774 lines of code (`discover.py:1206`, `seed_fetcher.py:488`,
  `pacer.py:80`) with 678 lines of tests, imported by nothing in `app/`. Writes CSV to
  `backend/data/instrument_master/` (`scripts/instrument_master/discover.py:629,661`).
- `scratch/` — 428 lines across 5 files, zero tests, committed to git.
- `demo_streaming/api.py` (262 lines) — **zero test coverage**; `tests/test_demo_streaming.py`
  exercises `publisher`, `snapshot`, and `stream` but never imports `create_demo_app`.

---

### 1.4 Data stores and external services actually touched

| Store / service | Reached by | Evidence |
|---|---|---|
| **PostgreSQL** (`asyncpg`, port 5433) | E1, E2, E4, E6, E8 | engine `app/db/session.py:16-31`; pool 20 + 30 overflow `:22-26`. E2 builds a *second* independent engine at `demo_streaming/main.py:33` |
| **IBKR TWS/Gateway socket** | E1 only (plus E6/E8 scripts) | `app/broker/ibkr/tws_client.py:494-547`; `placeOrder` at `app/oms/ibkr_adapter.py:257`; `cancelOrder` `:333`; `reqPositions` `app/broker/ibkr/tws_client.py:455`; `reqMktData` via `app/services/pnl.py:314,336,445,456,532` |
| **Redis** | E2 only | `demo_streaming/main.py:36-41`, `demo_streaming/stream.py:26-44`. Never constructed in E1 — see F-04 |
| **Local filesystem — webhook captures** | E1 | `app/api/routes/webhooks.py:26-28` → `backend/data/tradingview_webhooks/` (currently 1115 files); CSV appended at `:127-141` |
| **Local filesystem — logs** | E1, E2, E3 | `app/core/logger.py:19` → `/home/tradingapp/storage/logs/{date}/`; supervisor mirrors this at `/home/tradingapp/app/scripts/process_manager.py:52-53` |
| **HTTP → trading API** | E2 | `httpx` proxy at `demo_streaming/api.py:227-233` |
| **`/proc` + process table** | E1 | `psutil` in `app/services/system_monitor_service.py`; `pgrep`/`os.kill` in `/home/tradingapp/app/scripts/process_manager.py:259-291` |
| **Xvfb / IBC / Java** | E3 | `/home/tradingapp/app/scripts/process_manager.py:226-303` |

Postgres tables are defined across 14 models in `app/db/models/` and accessed through 11
repositories in `app/db/repositories/`.

---

### 1.5 Configuration inventory

Two independent settings classes exist. **Neither is cached.**

**`Settings`** — `app/core/config.py:10-103`. Loads `.env` from the process CWD
(`:18-23`, `extra="ignore"` at `:22`).

| Env var | Field:line | Read at |
|---|---|---|
| `APP_NAME` | `:26` | `app/main.py:173` |
| `ENVIRONMENT` | `:27` | **nowhere** |
| `LOG_LEVEL` | `:28` | `app/main.py:38` |
| `DATABASE_URL` | `:31` | `app/db/session.py:20` |
| `IBKR_HOST` | `:36` | `app/main.py:53,92,146,152`; `app/services/system_monitor_service.py` |
| `IBKR_PORT` | `:37` | `app/main.py:54,94`; `app/services/order_manager.py:168,270`; `app/services/system_monitor_service.py` |
| `IBKR_CLIENT_ID` | `:38` | `app/main.py:55,94` |
| `IBKR_CONNECTION_TIMEOUT` | `:39` | `app/main.py:56,95` |
| `IBKR_GATEWAY_MAX_MSG_PER_SEC` | `:42` | `app/main.py:44` |
| `IBKR_GATEWAY_NORMAL_MSG_PER_SEC` | `:43` | `app/main.py:45` |
| `IBKR_GATEWAY_EMERGENCY_RESERVE_PER_SEC` | `:44` | `app/main.py:46` |
| `IBKR_GATEWAY_MAX_WAIT_SEC` | `:45` | `app/main.py:47` |
| `IBKR_GATEWAY_ERROR100_COOLDOWN_SEC` | `:46` | `app/main.py:48` |
| `IBKR_MARKET_DATA_TYPE` | `:49` | **nowhere** |
| `IBKR_MARKET_DATA_SYMBOL` | `:50` | `scripts/test_tws_market_data.py:73` only |
| `IBKR_MARKET_DATA_SEC_TYPE` | `:51` | **nowhere** |
| `IBKR_MARKET_DATA_EXCHANGE` | `:52` | `scripts/test_tws_market_data.py:74` only |
| `IBKR_MARKET_DATA_CURRENCY` | `:53` | **nowhere** |
| `IBKR_MARKET_DATA_PRIMARY_EXCHANGE` | `:54` | **nowhere** |
| `TRADING_SYMBOL` | `:57` | `app/main.py:64` (feeds a dead code path — F-07) |
| `CANDLE_TIMEFRAME` | `:58` | only its own property at `:84-94` |
| `STRATEGY_CANDLE_COUNT` | `:59` | **nowhere** |
| `ORDER_QUANTITY` | `:60` | `app/main.py:65` (feeds a dead code path — F-07) |
| `MODEL_BLUE_COMMITTED_NOTIONAL` | `:67` | **nowhere** — F-06 |
| `PAPER_EXECUTE_STK_AS_CFD` | `:72` | `app/instruments/execution_override.py:19` |
| `WEBHOOK_AUTH_SECRET` | `:75` | `app/api/routes/webhooks.py:151` |
| `WEBHOOK_AUTH_ENABLED` | `:76` | `app/api/routes/webhooks.py:147` |
| `EMERGENCY_KILLSWITCH_AUTH_SECRET` | `:79` | `app/api/routes/emergency.py:39` |
| `EMERGENCY_KILLSWITCH_AUTH_ENABLED` | `:80` | `app/api/routes/emergency.py:35` |

**`DemoStreamSettings`** — `demo_streaming/config.py:6-23`: `DATABASE_URL`, `REDIS_URL`,
`DEMO_STREAM_HOST`, `DEMO_STREAM_PORT`, `DEMO_POLL_INTERVAL_MS`, `DEMO_SIGNAL_WATCH_LIMIT`,
`DEMO_PNL_EMIT_INTERVAL_MS`, `DEMO_STREAM_MAXLEN`, `DEMO_STREAM_NAME`, `TRADING_API_URL`.
Note `DATABASE_URL` is declared independently at `:14` rather than reusing `Settings`.

**Zero uses of `os.environ` / `os.getenv`** anywhere in `app/`, `demo_streaming/`, `scripts/`,
`scratch/`, or `alembic/`. All config flows through pydantic-settings. `process_manager.py`
hardcodes its own configuration block instead (`:51-106`).

**Hardcoded values that behave as configuration:**

| Value | Location |
|---|---|
| Worker pool size `10` | `app/main.py:126` (also the default at `app/services/worker_pool.py:58`) |
| Lease 30 s / reclaim 15 s / stale-claim 300 s / poll 0.5 s | `app/services/worker_pool.py:59-62` |
| Basket `fill_timeout=90.0`, `cancel_timeout=30.0` | `app/services/order_manager.py:164-165` |
| `_STK_CONTRACT_MONTH = "2026-09"` | `app/services/order_manager.py:78` |
| Default `max_open_positions=100`, `money_limit_per_symbol=10_000_000` | `app/services/order_manager.py:132-133`, `:1083-1084` |
| Gateway API port `4002` | `/home/tradingapp/app/scripts/process_manager.py:100` — separate from `IBKR_PORT` |
| FastAPI host/port `127.0.0.1:8000` | `/home/tradingapp/app/scripts/process_manager.py:57-58` |
| Postgres DSN with credentials | `app/core/config.py:31-33`, `alembic.ini:90`, `app/docker-compose.yml:9-10`, `demo_streaming/config.py:14` — four copies |

---

## Phase 1 — Defects found

Ordered by consequence. Every item is a code-level observation, not a doc comparison.

### F-01 — The committed tree does not match the running code

`git status` in `/home/tradingapp/app` shows 30+ modified files including `app/main.py`,
`app/oms/coordinator.py`, `app/oms/ibkr_adapter.py`, `app/services/pnl.py`,
`app/broker/ibkr/tws_client.py`, `app/core/config.py`, plus two **uncommitted deletions**:
`backend/app/broker/ibkr/scheduler.py` and `backend/app/oms/submit_pacer.py`. `HEAD` is
`6704d97 added process_manager`.

Consequence: `HEAD` is not deployable and no commit describes the audited behaviour. (Both deleted
modules are cleanly gone — no dangling references remain anywhere in `app/`, `tests/`, `scripts/`,
`scratch/`, or `demo_streaming/`.)

### F-02 — Unauthenticated reverse proxy exposes the entire order-control API

`demo_streaming/api.py:210-241` registers `ANY /api/v1/{full_path:path}` and forwards
`GET/POST/PATCH/PUT/DELETE` to the trading API with **no authentication and no path allow-list**.

Meanwhile **no route under `/api/v1` performs any authentication**. Grepping
`config.py`, `reconcile.py`, `baskets.py`, `orders.py`, `system_monitor.py` for
`Depends`/`Security`/`auth`/`token` yields only `get_db_session`, `get_oms`, `get_order_manager`.
The only two authenticated endpoints in the whole app are
`POST /api/webhooks/tradingview` (`app/api/routes/webhooks.py:176`) and
`POST /api/v1/emergency-kill-switch` (`app/api/routes/emergency.py:93`).

Endpoints reachable through the proxy that move money or mutate risk config:

| Endpoint | Location |
|---|---|
| `POST /accounts/{id}/square-off` | `app/api/routes/config.py:151-152` |
| `POST /accounts/{id}/positions/{trade_id}/close` | `app/api/routes/config.py:259-260` |
| `POST /accounts/{id}/kill-switch/clear` | `app/api/routes/config.py:194-195` |
| `POST /reconcile/positions/flatten` | `app/api/routes/reconcile.py:42-44` |
| `DELETE /orders/{order_id}` | `app/api/routes/orders.py:45-48` |
| `PUT /accounts/{id}/symbol-limits/{symbol}` | `app/api/routes/config.py:540-541` |
| `PUT /accounts/{id}/default-symbol-limit` | `app/api/routes/config.py:573-574` |
| `PATCH /allocations/{id}` | `app/api/routes/config.py:499-500` |
| `PATCH /execution` | `app/api/routes/config.py:675-676` |
| `DELETE /accounts/{id}` | `app/api/routes/config.py:467-468` |

E1 binds `127.0.0.1` (`process_manager.py:57`), which is the only thing containing this — and
`/home/tradingapp/start.txt` breaks that containment twice: `:3` puts ngrok on `:8000`, and `:9`
runs E2 with `DEMO_STREAM_HOST=0.0.0.0` on `:8010`. The proxy also forwards the client's
`Authorization` header verbatim (`demo_streaming/api.py:221-225`), so it is a credential-relay
path to the one Bearer-protected endpoint as well.

`demo_streaming/api.py` has **zero test coverage** (1.3), so none of this is asserted anywhere.

### F-03 — `OrderManager._tws_client()` always returns `None` (wrong attribute name)

`app/services/order_manager.py:1140-1143`:

```1140:1143:app/backend/app/services/order_manager.py
    def _tws_client(self):
        oms = self._oms
        adapter = getattr(oms, "adapter", None) if oms is not None else None
        return getattr(adapter, "_client", None)
```

`OMSService` stores the adapter as `self._adapter` (`app/oms/oms_service.py:29`). There is no
`adapter` property or attribute on `OMSService`. Every other reader in the codebase uses the
correct private name: `app/oms/coordinator.py:83`, `:481`, `:552`, and
`app/services/recovery.py:65`.

Because both `getattr` calls have `None` defaults, this fails **silently**. The two call sites both
pass the result as the TWS client for CFD contract discovery:

- `app/services/order_manager.py:1118` — inside `_resolve_instruments`, on the live execution path
- `app/services/order_manager.py:309` — inside `hydrate_live_pnl`, on startup

`discover_and_upsert_cfd` short-circuits on `client is None`
(`app/instruments/cfd_discover.py:124-129`) and logs
`"CFD discover skipped: ... reason=TWS not connected"`. So on-demand CFD conId discovery is
permanently dead, and the log line blames the TWS connection rather than the bug. Any CFD symbol
absent from the `instruments` table will fail resolution at
`app/services/order_manager.py:1128-1130` instead of being discovered.

### F-04 — System Monitor reads two `app.state` keys that are never set

`app/api/routes/system_monitor.py:26-27`:

```26:27:app/backend/app/api/routes/system_monitor.py
    tws_client = getattr(request.app.state, "tws_client", None)
    redis_client = getattr(request.app.state, "redis_client", None)
```

`app/main.py` sets `session_factory`, `client`, `ibkr_adapter`, `oms`, `order_manager`,
`worker_pool`, `position_reconciler`, `critical_recovery` (`:88`, `:106-110`, `:129`, `:133`).
It sets **`client`, not `tws_client`**, and never sets `redis_client` at all — E1 constructs no
Redis client anywhere (grep for `redis` in `app/` returns only the schema field, the two `getattr`
calls above, and the health-check helper).

Both are silent `None` defaults, so:

- `_check_ib_gateway_health(None, ...)` — the `tws_client is not None` branch at
  `app/services/system_monitor_service.py:306` can never be taken.
- `_check_redis_health(None)` — the `redis_client is not None` branch at
  `app/services/system_monitor_service.py:402` can never be taken.

The endpoint therefore reports a degraded/fallback status for both services regardless of their
real state. `pyproject.toml:17` carries `redis>=5.2.1` as a hard dependency of the trading app for
a client it never instantiates.

### F-05 — E1 shutdown is not in a `finally`, and two background owners are never stopped

`app/main.py:157-165`: `yield` at `:157` is followed by teardown at `:159-165` with no
`try:`/`finally:`. If the ASGI server raises out of the request-serving phase, or if any of the 15
startup steps between `:37` and `:155` raises, the teardown never runs — leaving the
`TWSClientThread` (`app/broker/ibkr/tws_client.py:531-532`), the 11 worker-pool tasks, and the
reconciler task alive.

Even on the clean path, teardown is incomplete:

- `CriticalRecoveryService` has **no `stop()`**. It spawns tasks with `asyncio.create_task` at
  `app/services/critical_recovery.py:107`, tracked in `self._in_flight`
  (`:84`). `app/main.py:159-165` never cancels them. Recovery tasks that call
  `_flatten_leftovers` (`:373`) can therefore be submitting orders while the TWS socket is being
  torn down at `app/main.py:164`.
- `LivePnlService` is never told to unsubscribe. There is no call to `unwatch`
  (`app/services/pnl.py:203`) or any bulk cancel during shutdown; the code just closes the socket.

### F-06 — `MODEL_BLUE_COMMITTED_NOTIONAL` is documented in code as a sizing gate but has zero readers

`app/core/config.py:62-67` carries a six-line comment asserting the field's behaviour:

```62:67:app/backend/app/core/config.py
    # TEMPORARY paper-testing Model Blue base-leg committed notional (USD).
    # Not live-account allocation and not a production financial default.
    # Unset/None => Model Blue OPEN is rejected (no invented size).
    # Replace later with database/OEMS CommittedCapitalProvider.
    # Env: MODEL_BLUE_COMMITTED_NOTIONAL
    model_blue_committed_notional: Decimal | None = None
```

Grepping the identifier across `app/`, `demo_streaming/`, `scripts/`, `scratch/`, and `tests/`
returns exactly one hit: the declaration itself. Nothing reads it. Sizing actually comes from
`DatabaseCommittedCapitalProvider` (`app/main.py:67`). Setting this env var has no effect, and the
comment's stated invariant ("Unset/None ⇒ Model Blue OPEN is rejected") is not enforced by this
field.

Same class of dead weight, without the misleading comment: `environment` (`:27`),
`ibkr_market_data_type` (`:49`), `ibkr_market_data_sec_type` (`:51`),
`ibkr_market_data_currency` (`:53`), `ibkr_market_data_primary_exchange` (`:54`),
`strategy_candle_count` (`:59`), `candle_timeframe` (`:58`) and its
`candle_timeframe_minutes` property (`:84-94`) — 15 lines of parsing logic with no caller.

### F-07 — The legacy single-name execution path is unreachable from E1, but its config is still wired

`app/services/order_manager.py:407-414` only reaches `_process_legacy_single_name` (`:511-579`,
69 lines) when `self._account_router is None`. But `__init__` guarantees a router whenever a
session factory is supplied (`:120-125`):

```120:125:app/backend/app/services/order_manager.py
        if account_router is not None:
            self._account_router = account_router
        elif session_factory is not None:
            self._account_router = DatabaseStrategyAccountRouter(session_factory)
        else:
            self._account_router = None
```

`app/main.py:69` passes `session_factory=AsyncSessionLocal`, so in E1 the router is always present
and the legacy path is dead. Nothing outside `order_manager.py` references
`_process_legacy_single_name`.

Consequences that remain live:

- `TRADING_SYMBOL` (`app/core/config.py:57`) and `ORDER_QUANTITY` (`:60`) are plumbed through
  `app/main.py:64-65` into `_symbol`/`_quantity`, which only that dead method reads.
- The no-handler branch of `_update_runtime_state` (`app/services/order_manager.py:960-976`)
  maintains a **second, incompatible keying scheme** for `open_positions`: keyed on bare
  `symbol`/`strategy_id` strings and incremented by *quantity*, whereas the live branch
  (`:936-939`) keys on the `open_position_key(intent)` tuple and counts *positions*. The same
  dict holds both shapes. `_process_legacy_single_name:536-538` reads the string-keyed form.
  `PositionLimitCheck` reads whichever it finds.

### F-08 — `OMSService` state grows without bound

`app/oms/oms_service.py:30-31` declares `self._orders: dict[str, OMSOrder]` and
`self._submitted_signals: set[str]`. Entries are added at `:249`, `:268`, `:284`, `:354`
(`_orders`) and `:112`, `:179` (`_submitted_signals`). **Nothing ever removes an entry** — there is
no eviction, TTL, or size cap in the file.

E1 is designed to run for days (`process_manager.py:24-25` describes it as long-lived under
systemd). Each `OMSOrder` retains a full `OrderIntent` including every `OrderLeg` and resolved
contract (`app/oms/oms_service.py:253`), so the retained set is not small. `GET /api/v1/orders`
(`app/api/routes/orders.py:21-26`) serialises the entire dict on every call with no pagination,
so the endpoint degrades along with memory.

The duplicate barrier built on `_submitted_signals` (`:77-93`) is also process-local and lost on
restart; the durable equivalent is `execution_claims`
(`app/services/order_manager.py:581-601`), making the in-memory check redundant for correctness
while retaining the leak.

### F-09 — `type(oms) is OMSService` silently disables the basket coordinator

`app/services/order_manager.py:160`:

```160:169:app/backend/app/services/order_manager.py
        if type(oms) is OMSService:
            self._baskets = BasketCoordinator(
```

An exact-type identity check, not `isinstance`. Any subclass, wrapper, or test double leaves
`self._baskets` as `None` (`:150`), which silently reroutes execution from the multi-leg basket
path (`:746-784`) to the single-shot `_oms.submit_intent` path (`:786-801`).

The two paths are not equivalent: only the basket path performs fill waiting
(`app/oms/coordinator.py:327`), leg-completeness checks (`:351`), compensation (`:820`),
`CRITICAL` marking (`:912`), and basket/order/execution persistence (`:951-1001`). A partially
filled multi-leg trade on the fallback path leaves naked exposure with no compensation and no
`CRITICAL` latch. The behaviour switch is invisible: no log line, no warning.

### F-10 — `get_settings()` constructs a fresh `Settings` on every call, including per-leg on the execution path

`app/core/config.py:97-103` returns `Settings()` with no `functools.lru_cache`. Because
`model_config` sets `env_file=".env"` (`:19`), each construction re-reads and re-parses `.env`
from disk and re-runs pydantic validation.

`app/instruments/execution_override.py:16-19` calls `get_settings()` on every invocation, and
`execution_instrument_type` calls it for every leg (`:34`). Call sites on the hot path:

- `app/instruments/resolver.py:157` and `:161` — per leg during resolution
- `app/services/order_manager.py:1107` — per leg in `_resolve_instruments`
- `app/services/order_manager.py:1164` — per leg in `_instrument_snapshot_for_legs`
- `app/services/order_manager.py:1188` — per leg in `_audit_instruments_resolved`

That is blocking synchronous file I/O inside the asyncio event loop, several times per order, in
the milliseconds before `placeOrder`. It also means config can change mid-trade: two legs of the
same basket can observe different `PAPER_EXECUTE_STK_AS_CFD` values if `.env` is edited between
calls.

### F-11 — `process_manager` exempts FastAPI from the crash-loop budget it advertises

The module docstring states restarts are rate-limited "to avoid crash-loops silently hammering
IBKR's servers" (`/home/tradingapp/app/scripts/process_manager.py:20-21`), implemented as
`restart_budget_exceeded` (`:188-191`) and enforced in `_restart` (`:579-588`).

FastAPI never goes through `_restart`. `_ensure_fastapi` (`:515-533`) calls `self.fastapi.start()`
directly at `:532` and never calls `record_restart()` or checks the budget. The supervision loop
reaches it via `:646-647` every 5 s (`:86`). A FastAPI process that crashes on startup —
e.g. because Postgres is down, since `app/main.py` has no DB readiness gate — will be respawned
every 5 seconds indefinitely, each attempt opening a fresh TWS socket session at
`app/main.py:91-96`.

Related: `fastapi_healthy()` (`:411-430`) is called at exactly one place, `:652`, whose entire body
is `log.debug(...)` (`:657`). An alive-but-wedged FastAPI is never acted on. And the health check
it performs hits `/health`, which is a static literal — `app/api/routes/health.py:8-11` returns
`{"status": "ok"}` with no DB or IB check — contradicting the comment at
`process_manager.py:59-60` and the docstring at `:412-417` that assume otherwise.

Also `:169`: `log_fh = open(log_path, "a", buffering=1)` is never closed and not stored on the
dataclass — one leaked file descriptor per process start/restart.

### F-12 — `.env.example` is git-ignored, stale, and documents two variables that do not exist

`app/.gitignore:22` ignores `backend/.env.*`, which matches
`backend/.env.example`. Confirmed: `git ls-files --error-unmatch backend/.env.example` fails. The
only configuration template in the repo is not in the repo.

Its content is also wrong. `backend/.env.example:10` sets `BROKER_MODE=mock` and `:29` sets
`ALLOCATIONS_CONFIG_PATH=config/paper_allocations.yaml`. Neither exists as a field on `Settings`
(`app/core/config.py:10-80`), and `extra="ignore"` (`:22`) means both are silently discarded — a
reader configuring `BROKER_MODE=mock` gets live IBKR wiring. Simultaneously the file omits every
security-relevant and operationally-relevant variable that *is* read: `DATABASE_URL`,
`WEBHOOK_AUTH_SECRET`, `WEBHOOK_AUTH_ENABLED`, `EMERGENCY_KILLSWITCH_AUTH_SECRET`,
`EMERGENCY_KILLSWITCH_AUTH_ENABLED`, all five `IBKR_GATEWAY_*` pacing knobs, and
`PAPER_EXECUTE_STK_AS_CFD`.

The live `backend/.env` sets `BROKER_MODE` (ignored) and omits
`EMERGENCY_KILLSWITCH_AUTH_SECRET` entirely — with `emergency_killswitch_auth_enabled`
defaulting to `True` (`app/core/config.py:80`), `app/api/routes/emergency.py:40-47` fails closed,
so the emergency kill switch endpoint currently returns 401 for every request.

### F-13 — Four independent copies of the Postgres DSN, including credentials

`app/core/config.py:31-33`, `alembic.ini:90`, `app/docker-compose.yml:9-10`, and
`demo_streaming/config.py:14` each carry `root`/`root123`/`5433`/`ibkr_trading`. `alembic/env.py`
does import `app.core.config`, so the `alembic.ini:90` value is likely overridden at runtime — but
it remains a plaintext credential in a tracked file, and the four defaults can drift
independently.

Similarly the IB Gateway API port is specified twice with different values:
`process_manager.py:100` hardcodes `4002` while `app/core/config.py:37` defaults `IBKR_PORT` to
`7497`. The comment at `process_manager.py:98` acknowledges they "must match" but nothing checks
it — the supervisor will declare the Gateway ready on `4002` and then start a FastAPI that
connects to whatever `IBKR_PORT` says.

### F-14 — Dead code inside live modules

- `app/api/routes/webhooks.py:59-63` — `_save_raw_capture_file` is defined and never called.
  Grepping the identifier across `app/`, `tests/`, and `scripts/` returns only the definition.
  The per-request JSON capture it implies does not happen; captures go into the DB row
  (`:250-257`) and the CSV (`:282-296`).
- `demo_streaming/api.py:68` — `config_base` is computed and never read; the proxy at `:216`
  recomputes its own base URL.
- `app/services/order_manager.py:978-1002` — `record_rejected_inbound` (25 lines) has no caller
  anywhere in `app/`, `scripts/`, or `demo_streaming/`.
- `app/services/order_manager.py:355-364` — `process_signal` is called only by
  `scripts/oms/run_paper_execution.py:171`.
- `backend/logs/trading.log` — 1.6 MB, last written 2026-08-18, orphaned. `app/core/logger.py:19`
  resolves `LOG_DIR` to `/home/tradingapp/storage/logs`, so nothing writes here any more.

### F-15 — Smaller structural problems

- **Private-attribute wiring from `main.py`.** `app/main.py:72` assigns
  `order_manager._live_pnl`, and `:85-87` reach into `order_manager._baskets`. Neither is a
  constructor parameter (`app/services/order_manager.py:92-109`), so the object graph cannot be
  built correctly by any other caller — including tests and `scripts/oms/run_paper_execution.py`.
- **Order-dependent startup.** `hydrate_runtime_from_db()` runs at `app/main.py:76`, before
  `CriticalRecoveryService` exists (`:80`) and before `set_recovery_service` (`:86`). Inside,
  `app/services/order_manager.py:202` calls `hydrate_critical_from_db`, whose
  `enqueue_all_critical` branch is guarded by `self._recovery_service is not None`
  (`app/oms/coordinator.py:192-193`) and is therefore always skipped. `app/main.py:135`
  compensates by calling `enqueue_all_critical()` manually. Reordering these lines silently
  double-enqueues or silently stops enqueuing.
- **Instantiating a repository without `__init__`.** `app/services/pnl.py:176`:
  `helper = PositionRepository.__new__(PositionRepository)`, then calls `helper.to_open_trade(row)`
  at `:178`. This works only as long as `to_open_trade` never touches instance state; the moment it
  does, this raises `AttributeError` on the startup P&L rehydration path.
- **Redundant work per order.** `attach_resolved` runs twice on the same intent —
  `app/services/order_manager.py:1128` then `app/oms/coordinator.py:205`. `_persist_child` runs
  three times per order for a basket that settles cleanly — `app/oms/coordinator.py:275`
  (in the submit loop), `:329` (after the fill wait), `:374` (after the completeness check) —
  plus a fourth at `:362` for any retried leg.
- **`abort_remaining` is set but never honoured for the current iteration.**
  `app/oms/coordinator.py:325` sets it after the leg has already been submitted; the guard at
  `:264` only affects subsequent legs. On a 2-leg basket, a rejection on leg 0 aborts leg 1, but a
  rejection on leg 1 sets a flag that is never read.
- **Duplicate `TWSClient` re-export facades.** `app/broker/__init__.py:3` and
  `app/broker/ibkr/__init__.py:3` export the same symbol; no `app/` module imports either.
- **Parallel in-memory/DB implementations kept side by side** with no shared interface enforcement:
  `app/services/model_blue/allocation.py` vs `db_allocation.py`,
  `app/services/model_blue/trade_book.py` vs `db_trade_book.py`,
  `app/services/recovery.py` vs `app/services/critical_recovery.py`,
  `app/services/reconcile_service.py` vs `app/services/position_reconciler.py`. Only the DB
  variants are wired in E1 (`app/main.py:67-68`, `:116`, `:131`).

---

## Unverified / not examined in Phase 1

Stated explicitly so nothing here is mistaken for a finding:

- Runtime behaviour. No process was started; no test suite was run; no log file was read. All
  findings are static.
- `app/api/routes/config.py` (717 lines) — route surface and auth posture enumerated; handler
  bodies not audited.
- `app/oms/coordinator.py:377-1186` — retry, compensation, `_fail_critical`, and persistence
  internals read only at signature level.
- `app/services/pnl.py`, `app/services/kill_switch.py`, `app/services/position_reconciler.py`,
  `app/services/critical_recovery.py`, `app/services/system_monitor_service.py`,
  `app/accounts/config_service.py`, `demo_streaming/snapshot.py` — surveyed for wiring and
  lifecycle only; internal correctness not assessed.
- All 11 repositories in `app/db/repositories/` — SQL not reviewed. In particular the fencing and
  locking semantics of `claim_next_jobs` (`signal_repository.py:347`), `update_status` (`:421`),
  `heartbeat_lease` (`:464`), and `reclaim_stale_jobs` (`:486`) are load-bearing for F-08's
  duplicate-suppression argument and were **not** verified.
- The entire frontend (`app/frontend/src`, 43 files) — only `vite.config.ts` and the four
  `src/api/*.ts` base paths were read, to establish which backend routes the UI depends on.
- `app/rms/checks/*` — check logic not evaluated.
- Whether `PositionLimitCheck` actually misreads the dual-keyed `open_positions` dict described in
  F-07. The two writer shapes are verified; the reader was not traced.
- Alembic migration bodies — only the `revision`/`down_revision` chain was verified as linear.

## Phase 2 (docs vs. code) — not started

Per instructions, `AGENTS.md`, `app/docs/*`, `Execution_System_Architecture.md`, and
`.cursor/rules/*` were excluded from Phase 1 and have not been compared against the map above.
Three contradictions were nonetheless unavoidable and are logged here for Phase 2 rather than
claimed as findings: `/home/tradingapp/AGENTS.md` links to
`Execution_System_Architecture.md`, which does not exist at the repo root; and
`.cursor/rules/backend-python.mdc` instructs the reader to ignore `backend/main.py`, which does not
exist, while `.cursor/rules/trading-safety.mdc` asserts that kill-switch and flatten HTTP APIs
"are not in the routers" — `app/api/routes/emergency.py:77`, `app/api/routes/config.py:194`, and
`app/api/routes/reconcile.py:42` are all present.
