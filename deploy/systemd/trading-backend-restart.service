[Unit]
Description=Restart Trading Backend via trigger (one-way Gateway → Backend)
After=trading-backend.service

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'touch /tmp/backend_auto_restarting; /usr/bin/systemctl restart trading-backend.service; sleep 2; rm -f /tmp/backend_auto_restarting'
